# Rust / Tauri
target/
gui/src-tauri/gen/

# Node / Vite (GUI frontend)
node_modules/
gui/dist/

# IDE
.vs/
.vscode/
.idea/

# Build artifacts
*.vpk
dist/

# Scratch space: preview renders, working images, ad-hoc mod dirs
.scratch/
.scratch_*.png
mods/

# morphic: gitignored extended corpus (committed canonical corpus lives next to it)
morphic/fixtures/_local/

# .NET (morphic-oracle dev tool)
tools/morphic-oracle/bin/
tools/morphic-oracle/obj/

# Local agent / session state (Claude Code worktrees, transcripts, caches)
.claude/

# Local Blender renders, bakes, and GLB exports (large, not source)
blender-work/

# Branding concept dump (kept local, not part of the published tool)
logo-concepts/

# Repository paper, kept local for now (LaTeX source + built PDF)
docs/vpkmerge-repository-paper.tex
docs/vpkmerge-repository-paper.pdf
